//MENU HAMBURGUESA


#include "stdafx.h"
#include <iostream>
#include "conio.h"
#include "math.h"

using namespace std;

void main()
{
	float acum=0,a;

		cout<<"Bienvenido a Hamburguesas FERTELL permitanos tomar su orden...:"<<endl;
	    cout<<"Desea pedir una hamburguesa si=1, no=2?"<<endl;
		cin>>a;
		if (a==1)
			acum=acum+3;
		else
			acum=acum;
		cout<<"Desea agregarle queso si=1, no=2?"<<endl;
		cin>>a;
		if (a==1)
			acum=acum+2;
		else
			acum=acum;
		cout<<"Desea papas si=1, no=2"<<endl;
		cin>> a;
		if(a==1)
			acum=acum+2.5;
		else
			acum=acum;
		cout<<"Desea malteadas? si=1,no=2"<<endl;
		cin>>a;
		if(a==1)
			acum=acum+4;
		else
			acum=acum;
		cout<<"Desea soda si=1,no=2?"<<endl;
		if(a==1)
			acum=acum+2.5;
		else
			acum=acum;


		cout<<"su cuenta es: "<<acum<<"Bs"<<endl;

		getch();
}
